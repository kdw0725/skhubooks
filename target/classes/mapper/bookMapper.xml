<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skhu.skhubooks.bookMapper"><!-- 이거 수정해줘야된다 동욱아 -->
	<select id="selectBook" resultType="bookVO" parameterType="hashMap">
		SELECT 
							BOOK_NO,
							BOOK_NAME, 
							BOOK_AUTHOR, 
							BOOK_PUBLISHER, 
							BOOK_LOAN 
		FROM 
							BOOK 
		WHERE 
							BOOK_NAME IS NOT NULL
		LIMIT
        					#{pageStart}, #{perPageNum}
	</select>
	
	<select id="bookTotalCount" resultType="Integer">
		SELECT
							COUNT(BOOK_NO)
		FROM	
							BOOK
		WHERE
							BOOK_NO IS NOT NULL
	</select>
	<insert id="insertBook" parameterType="bookVO">
		INSERT 
		INTO 
							BOOK(
							BOOK_NO, 
							BOOK_NAME, 
							BOOK_AUTHOR, 
							BOOK_PUBLISHER
							)
		VALUES
							(
							#{book_no},
							#{book_name},
							#{book_author},
							#{book_publisher}
							)
	</insert>
</mapper>